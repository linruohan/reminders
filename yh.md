# Reminders 项目 — 未完善与可优化点

> 审查日期：2026-07-24  
> 范围：前端 `src/`、后端 `src-tauri/`、文档与工具链  
> **进度更新**：P0 已全部落地；P1 核心体验项已落地（表单抽取 / 虚拟化 / FTS 等仍待做）

---

## 一、总览

项目已具备列表管理、筛选、日历四视图、重复提醒、运行时系统通知等核心能力。对照 Apple Reminders，仍有长期差距（子任务、关闭应用后通知、同步等）。

```
已实现                              仍待 / 半成品
─────────────────────────────────────────────────
列表 + 智能筛选（含负责人/标签）      子任务
内联 + 弹窗编辑                      抽取统一 ReminderForm
重复（完成时生成下次，含 update）     按星期重复 / 多条提醒规则
提前提醒 + 运行时通知（已补扫）       关闭应用后通知 / 托盘常驻
日历四视图 + 双击预填时间             拖拽改期 / 完整 24h 轴
剪切复制粘贴（含 owner）              撤销 / iCloud 同步
旗标/优先级/URL/标签/全天             附件 / 位置提醒
```

---

## 二、P0 — 影响可用性

| # | 问题 | 状态 | 说明 |
|---|------|------|------|
| 1 | 「完成」筛选列表可能为空 | ✅ 已修 | `ReminderList`：`completed` 筛选不再隐藏已完成项 |
| 2 | 内联编辑标签不保存 | ✅ 已修 | `ReminderItemEditMode`：`addTag`/`removeTag` 写入 draft |
| 3 | 通过 update 标记完成不生成下次重复 | ✅ 已修 | 与 toggle 共用 `maybe_spawn_next_occurrence` |
| 4 | 通知可能漏发 | ✅ 已改善 | 窗口 90s→600s；启动立即补扫 |

---

## 三、P1 — 体验闭环（本轮已做）

| 项 | 状态 |
|----|------|
| 新建继承当前列表 + Toast | ✅ |
| 日历双击预填日期/时间；全天双击预填全天 | ✅ |
| 新建支持全天；编辑弹窗标题/URL 校验 | ✅ |
| 月视图选日切到日视图 | ✅ |
| 负责人侧边栏筛选 + 计数；粘贴保留 owner | ✅ |
| 创建提醒支持 `owner_id`（后端） | ✅ |
| 标签侧边栏筛选 + `get_reminders_by_tag` | ✅ |
| 列表计数改为未完成数 | ✅ |

### 仍待（P1 余量 / P2）

- 抽取共享 `ReminderForm`（消除 Add/Edit 重复）
- 列表虚拟化、FTS 搜索、按日期范围查询
- 子任务 / OS 级通知 / 托盘常驻 / 拖拽改期
- 文档 `design.md` 对齐实现；可选 lint/format

---

## 四、建议下一轮

1. 抽取 `ReminderForm`，统一三处编辑入口  
2. 时间轴扩展为 24h；日历拖拽改期  
3. 列表虚拟化 + 搜索 FTS5  
4. 归档过时文档，补 `package.json` lint/format 脚本  
